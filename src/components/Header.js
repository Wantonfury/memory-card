import React from "react";
import './css/Header.css';

const Header = () => {
    return (
        <div id="header">
            <h4>Pick a card.</h4>
            <span>However, do not pick the same card twice!</span>
        </div>
    );
}

export default Header;